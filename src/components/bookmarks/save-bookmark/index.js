import angular from 'angular';
import SaveBookmarkComponent from './save-bookmark.component';

export default angular
	.module('SaveBookmarkModule', [])
	.component('saveBookmark', SaveBookmarkComponent);

